// Stub for the aerosol flow-field animation (SPEC.md §5). The static hero
// must stand on its own before the canvas layer is built, so this only
// marks the first frame ready for tools/shot.py --wait.
window.heroReady = true;
