_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler — anchored to root so it doesn't also
# catch assets/js/vendor/, which is tracked on purpose (SPEC.md §2).
.bundle/
/vendor/
